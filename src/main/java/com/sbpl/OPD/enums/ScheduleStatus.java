package com.sbpl.OPD.enums;

public enum     ScheduleStatus {
    ACTIVE,       // Schedule is active and accepting appointments
    INACTIVE,     // Schedule is inactive
    SUSPENDED,    // Schedule temporarily suspended
    COMPLETED     // Schedule period has ended
}